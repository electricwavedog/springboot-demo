spring:
    datasource:
        url: jdbc:mysql://localhost:3306/spring_test
        username: root
        password: 123456
        driver-class-name: com.mysql.jdbc.Driver
    redis:
        database: 0
        host: localhost
        port: 6379
        password:
        timeout: 100
        pool:
            max-active: 8
            max-wait: -1
            max-idle: 8
            min-idle: 0
    data:
      mongodb:
        uri: mongodb://localhost/mongodbtest
mybatis:
    mapper-locations: classpath:mappers/*.xml
    typeAliasesPackage: com.example.test.model
flyway:
    baseline-on-migrate: true